# Copy to .env.local and fill in. Both values are public (anon key) and safe
# to ship to the browser; row-level security gates what they can read.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
